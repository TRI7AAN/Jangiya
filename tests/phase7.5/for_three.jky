# Fixture 3: literal bound of 3, one call in the body — exactly 3
# manifest entries for that call (independent attempts), not 1, not 0.
case t {
  allowed_capabilities: ["recon.fixture.execute"];
}
investigate t {
  for (int i = 0; i < 3; i++) {
    let x = call jky_recon_true_branch(target: "w");
    x | emit fin;
  }
}
